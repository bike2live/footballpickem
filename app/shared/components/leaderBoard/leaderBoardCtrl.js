app.controller('leaderBoardCtrl', function ($scope, $rootScope, $routeParams, $location, $http, $modal, $log, $filter, Data) {

    $log.debug('got to the leaderBoardCtrl');

    // get gameId from routeparams
    //$routeParams

});
