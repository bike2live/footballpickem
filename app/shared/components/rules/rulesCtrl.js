app.controller('rulesCtrl', function ($scope, $rootScope, $routeParams, $location, $http, $modal, $log, $filter, Data) {

    $log.debug('got to the rulesCtrl');

    // get gameId from routeparams
    //$routeParams

});
