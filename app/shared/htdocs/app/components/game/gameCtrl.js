app.controller('gameCtrl', function ($scope, $rootScope, $routeParams, $location, $http, $modal, $log, $filter, Data) {

    $log.debug('got to the gameCtrl');

    // get gameId from routeparams
    //$routeParams

});
